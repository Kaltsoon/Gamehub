<%= form_for(@genre_game) do |f| %>
  <% if @genre_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@genre_game.errors.count, "error") %> prohibited this genre_game from being saved:</h2>

      <ul>
      <% @genre_game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game_id %><br>
    <%= f.number_field :game_id %>
  </div>
  <div class="field">
    <%= f.label :genre_id %><br>
    <%= f.number_field :genre_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
